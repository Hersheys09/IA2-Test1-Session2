#include<stdio.h>

void input_string(char *a)
{
  printf("Enter the String:\n");
  for(int i=0; i++;)
    {
      scanf("%c", *a);
    }
}

char *str_reverse(char *a)
{
  for(int i=n-1, i>0, i--)
    {
      
    }
}

void output(char *a,char *reversea)
{
  printf("The reverse of string %s is %s", *a, *reversea);
}

int main()
{
  char a[20];
  input_string(&a);
  char reversea[20];
  str_reverse(&reversea);
  output(a, reversea);
  return 0;
}